/* NEXT */
// prepare component with input text tag, with props {value, onChange, ...(as many props in inputs you know)}
// in 2 ways - with class and with hooks
// component should log state changes in console

/* NEXT */
// prepare component with input range, with props {data=['a','b',...'z']}
// which shows below selected with range a letter
// in 2 ways - with class and with hooks

/* NEXT */
// prepare table component with state {columns, values}
// where have every tbody cell have input(text) with value and which can be edited
